name: hello_morning
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  permission_handler: ^11.0.1
  intl: ^0.18.1
  background_sms: ^0.0.1
  geolocator: ^10.1.0
  flutter_foreground_task: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - icon.png
    - assets/smile.png  # smile.png를 에셋 시스템에 등록합니다.
