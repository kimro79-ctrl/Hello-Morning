name: hello_morning
version: 1.2.50+250
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ✅ 로컬 경로 설정
  background_sms:
    path: ./plugins/background_sms
  contacts_service:
    path: ./plugins/contacts_service
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.19

flutter:
  uses-material-design: true
