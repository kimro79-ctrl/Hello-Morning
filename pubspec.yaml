name: daily_safety_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  permission_handler: ^11.3.0
  intl: ^0.19.0
  background_sms: ^0.0.1
  geolocator: ^11.0.0

flutter:
  uses-material-design: true
  # 사용자님의 Asset 경로를 다시 등록했습니다.
  assets:
    - assets/images/
