name: daily_safety_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  permission_handler: ^11.1.0
  intl: ^0.19.0
  background_sms: ^0.0.1
  geolocator: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # assets 바로 아래에 들여쓰기(공백 4칸)를 정확히 맞춰야 합니다.
  assets:
    - assets/smile.png
