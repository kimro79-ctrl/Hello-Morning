name: hello_morning
description: "하루 한번 안심 지키미"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  intl: ^0.19.0
  permission_handler: ^11.3.0
  url_launcher: ^6.2.5
  geolocator: ^11.0.0
  background_sms: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # assets 바로 앞은 공백 2칸, - 앞은 공백 4칸입니다.
  assets:
    - assets/smile.png
