name: hello_morning
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  contacts_service: ^0.6.3
  permission_handler: ^11.3.1
  intl: ^0.19.0
  background_sms: ^0.0.4
  geolocator: ^11.1.0
  http: ^1.1.0 # what3words 통신을 위해 반드시 필요

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/smile.png
