name: hello_morning
version: 1.2.50+250
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 로컬 플러그인 (경로 유지)
  background_sms:
    path: ./plugins/background_sms
  contacts_service:
    path: ./plugins/contacts_service

  # ✅ 빌드 에러 해결을 위해 반드시 추가되어야 함
  shared_preferences: ^2.2.2
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.19
  permission_handler: ^11.3.1
  intl: ^0.19.0
  geolocator: ^11.0.0
  flutter_foreground_task: ^6.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/smile.png
