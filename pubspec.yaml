name: hello_morning
description: "하루 한번 안심 지키미"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  intl: ^0.19.0
  permission_handler: ^11.3.0
  url_launcher: ^6.2.5
  geolocator: ^11.0.0
  background_sms: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # [수정] 파일명 대신 폴더명을 등록하여 인식 오류를 방지합니다.
  assets:
    - assets/
