name: hello_morning
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  contacts_service: ^0.6.3
  permission_handler: ^11.3.1
  intl: ^0.19.0
  background_sms: ^0.0.4
  geolocator: ^11.1.0
  http: ^1.1.0
  # ✅ SDK 버전을 올리지 않아도 되는 안정적인 버전으로 세팅
  flutter_foreground_task: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/smile.png
    - assets/icon/1인가구 안심 지키미.png
