name: hello_morning
description: 하루 안부 지킴이
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  contacts_service: ^0.6.3
  intl: ^0.19.0
  permission_handler: ^11.3.0
  flutter_sms: 2.3.3  # 오류 메시지에 따라 호환 버전으로 하향 조정
  workmanager: ^0.5.2

flutter:
  uses-material-design: true
  assets:
    - assets/smile.png
