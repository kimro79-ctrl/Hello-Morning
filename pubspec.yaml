name: hello_morning
description: "A new Flutter project."
publish_to: 'none'

version: 1.2.50+250

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # ✅ 외부 pub.dev가 아닌 내 폴더를 봅니다.
  background_sms:
    path: ./plugins/background_sms
  contacts_service:
    path: ./plugins/contacts_service

  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
